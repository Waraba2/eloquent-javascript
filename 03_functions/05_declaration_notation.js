// shorter way to declare a function
function square(x) {
  return x * x;
}

// Odering of the function call and function doesn't matter.
console.log("The future says:", future());
function future() {
  return "You'll never have flying cars.";
}
