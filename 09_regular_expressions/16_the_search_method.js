// indexOf using regex return the first instance the match or -1 if not found. Doesn's allow the 
// starting offset like indexOf
console.log("  word".search(/\S/));
// -> 2
console.log("    ".search(/\S/));
// -> -1 
